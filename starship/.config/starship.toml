# ~/dotfiles/starship/.config/starship.toml
[character]
success_symbol = "[➜](bold green)"
error_symbol = "[✗](bold red)"

[package]
disabled = true # Reduces clutter

[aws]
symbol = "☁️ "

[python]
symbol = "🐍 "

[conda]
symbol = "🅒 "
format = "[$symbol$environment]($style) " # Removed "via" for a cleaner look
ignore_base = true # Hides the prompt if you are in the 'base' env
